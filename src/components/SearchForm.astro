---
const q = Astro.url.searchParams.get('q') ?? '';
---

<form class="search" action="/search" method="get" role="search">
  <input
    type="search"
    name="q"
    placeholder="Search articles…"
    value={q}
    aria-label="Search"
  />
  <button type="submit">Search</button>
</form>

<style>
.search { display:flex; gap:.5rem; }
.search input { flex:1; padding:.5rem .75rem; border:1px solid #e5e7eb; border-radius:8px; }
.search button { padding:.5rem .75rem; border:1px solid #e5e7eb; border-radius:8px; background:#f8fafc; }
</style>
